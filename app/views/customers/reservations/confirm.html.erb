<div class="container">
  <div class="row">
    <div class="col-xs-10 col-xs-offset-1"><!-- 左右に余白、中央に配置 -->
      <h2>予約内容確認</h2>
      <p>予約内容を確認の上、「予約を確定する」ボタンをクリックしてください。</p><br>

      <%= form_with model:@reservation, local: true, url: customers_reservations_path do |f| %>
      <table class="table_singup">
        <tbody>

          <tr>
            <th class="pa10px40px">ジム名</th>
            <td>
              <div class="field">
                <%= @gym.gym_name %>
                <%= f.hidden_field :gym_id, :value => @gym.id %>
              </div>
            </td>
          </tr>
          <tr>

          <tr>
            <th class="pa10px40px">ご希望予約日・時間</th>
            <td>
              <div class="field">
                <%= @reservation.start_date %>
                <%= f.hidden_field :start_date, :value => @reservation.start_date %>
              </div>
            </td>
          </tr>

          <tr>
            <th class="pa10px40px">お名前</th>
            <td>
              <div class="field">
                <%= @reservation.full_name %>
                <%= f.hidden_field :full_name, :value => @reservation.full_name %>
              </div>
            </td>
          </tr>

          <tr>
            <th class="pa10px40px">メールアドレス</th>
            <td>
              <div class="field">
                <%= @reservation.email %>
                <%= f.hidden_field :email, :value => @reservation.email  %>
              </div>
            </td>
          </tr>

          <tr>
            <th class="pa10px40px">性別</th>
            <td>
              <div class="field">
                <%= @reservation.sex %>
                <%= f.hidden_field :sex, :value => @reservation.sex %>
              </div>
            </td>
          </tr>

          <tr>
            <th class="pa10px40px">年齢</th>
            <td>
              <div class="field">
                <%= @reservation.age %>
                <%= f.hidden_field :age, :value => @reservation.age %>
              </div>
            </td>
          </tr>

          <tr>
            <th class="pa10px40px">住所</th>
            <td>
              <div class="field">
                <%= @reservation.postal_code %>
                <%= f.hidden_field :postal_code, :value => @reservation.postal_code %>
                <%= @reservation.address %>
                <%= f.hidden_field :address, :value => @reservation.address %>
              </div>
            </td>
          </tr>

          <tr>
            <th class="pa10px40px">電話番号</th>
            <td>
              <div class="field">
                <%= @reservation.tell %>
                <%= f.hidden_field :tell, :value => @reservation.tell %>
              </div>
            </td>
          </tr>

          <tr>
            <td colspan="2">
              <div class="actions">
                <%= f.submit "予約を確定する", class:"btn btn-success btn-lg" %>
              </div>
            </td>
          </tr>
        </tbody>
      </table>
      <% end %>
    </div>
  </div>
</div>

