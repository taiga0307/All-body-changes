<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-10 col-sm-offset-1 col-md-10 col-md-offset-1 col-lg-10 col-lg-offset-1"><!-- 左右に余白、中央に配置 -->
      <h1>パスワード変更</h1>
      <p>パスワードを入力してください。この情報は後から変更することが出来ます。</p><br>

      <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

        <table class="table_singup">
          <tbody>
            <tr>
              <th class="table-title">メールアドレス<span class="required">【必須】</span></th>
              <td>
                <div class="field">
                  <%= f.email_field :email, autocomplete: "email" %>
                </div>
                <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
                  <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
                <% end %>
              </td>
            </tr>
            <tr>
              <th class="table-title">現在のパスワード<span class="required">【必須】</span></th>
              <td>
                <div class="field">
                  <%= f.password_field :current_password, autofocus: true, autocomplete: "current-password" %><br>半角英数字6〜12文字で入力してください
                </div>
                </div>
              </td>
            </tr>
            <tr>
              <th class="table-title">新しいパスワード<span class="required">【必須】</span></th>
              <td>
                <div class="field">
                  <%= f.password_field :password, autocomplete: "new-password" %><br>半角英数字6〜12文字で入力してください
                </div>
                  <!--  < if @minimum_password_length %>
                  <br />
                    <em><%= @minimum_password_length %> characters minimum</em>
                  < end %> -->
                </div>
              </td>
            </tr>
            <tr>
              <th class="table-title">新しいパスワード(確認用)<span class="required">【必須】</span></th>
              <td>
                <div class="field">
                  <%= f.password_field :password_confirmation, autocomplete: "new-password" %><br>半角英数字6〜12文字で入力してください
                </div>
                </div>
              </td>
            </tr>
            <tr>
            <td colspan="2">
                <div class="change-btn">
                  <%= f.submit "変更する", class:"btn btn-success btn"  %>
                </div>
              </td>
              <td></td>
            </tr>
          </tbody>
        </table>
      <% end %>
    </div>
  </div>
</div>


